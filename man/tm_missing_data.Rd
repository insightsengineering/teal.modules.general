% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tm_missing_data.R
\name{tm_missing_data}
\alias{tm_missing_data}
\title{Missing data module}
\usage{
tm_missing_data(label = "Missing data")
}
\arguments{
\item{label}{label shown in the navigation for the item}
}
\description{
Present analysis of missing observations and patients.
}
\examples{
library(random.cdisc.data)

ADSL <- radsl(cached = TRUE)
ADRS <- radrs(cached = TRUE)

app <- init(
  data = cdisc_data(
    cdisc_dataset("ADSL", ADSL),
    cdisc_dataset("ADRS", ADRS),
    code = "ADSL <- radsl(cached = TRUE); ADRS <- radrs(cached = TRUE)",
    check = TRUE
  ),
  modules = root_modules(
    tm_missing_data()
  )
)
\dontrun{
  shinyApp(app$ui, app$server)
}
}

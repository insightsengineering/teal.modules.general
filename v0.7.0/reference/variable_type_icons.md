# Get icons to represent variable types in dataset

Get icons to represent variable types in dataset

## Usage

``` r
variable_type_icons(var_type)
```

## Arguments

- var_type:

  (`character`) of R internal types (classes).

## Value

(`character`) vector of HTML icons corresponding to data type in each
column.
